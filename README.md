# Pokédex – React Native (Expo)

Um aplicativo simples construído com **React Native + Expo** que consome a **PokéAPI** e exibe uma lista de Pokémon com **rolagem infinita** (infinite scroll).  
Perfeito para estudo de requisições HTTP, listas com FlatList e integração com APIs públicas.

---

## Tecnologias usadas

- **React Native**
- **Expo**
- **JavaScript**
- **PokéAPI (API pública)**
- **FlatList com Infinite Scroll**

---

##  Objetivo do projeto

Este projeto foi desenvolvido como parte de um estudo de consumo de APIs em aplicativos móveis, utilizando React Native.  
O objetivo é permitir que o usuário navegue por uma lista de Pokémon carregada dinamicamente.

---

##  API utilizada

**PokéAPI**  
https://pokeapi.co/

Endpoint usado:

https://pokeapi.co/api/v2/pokemon

## Funcionalidades

- ✔ Lista de Pokémon  
- ✔ Consumo da API oficial  
- ✔ Rolagem infinita (carrega mais ao chegar no fim)  
- ✔ Mostra imagem e nome do Pokémon  
- ✔ Layout simples e responsivo  

---

# Como abrir e rodar este projeto React Native (Expo)

Este guia explica passo a passo como abrir e rodar o projeto **Pokédex** feito em React Native com Expo.

---

## 1. Pré-requisitos

Antes de rodar o projeto, instale:

### ✔ Node.js (obrigatório)
Baixe em:  
https://nodejs.org/

### ✔ Expo CLI
Após instalar o Node.js, execute no terminal:

```bash
npm install -g expo-cli

 ## 2. Baixar o projeto

Você pode escolher uma das opções abaixo:

### 🔹 Opção A — Baixar ZIP
1. Abra o repositório no GitHub  
2. Clique em **Code**  
3. Escolha **Download ZIP**  
4. Extraia o arquivo  
5. Abra a pasta no VS Code  

### 🔹 Opção B — Clonar com Git
Se tiver Git instalado, execute:

```bash
git clone https://github.com/DiogoBarbosa23/pokemon-app.git
```

Depois entre na pasta:

```bash
cd pokemon-app
```

---

## 3. Instalar dependências

Com o terminal aberto dentro da pasta do projeto, execute:

```bash
npm install
```

---

## 4. Rodar o projeto

Para iniciar a aplicação, execute:

```bash
npm start
```

Para rodar no navegador:  
No terminal/Expo, pressione **"w"**


Para iniciar a aplicação, execute:

npm start

Para rodar no navegador: no terminal/expo, pressione "w"
